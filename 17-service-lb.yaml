apiversion: v1
kind: Service
metadata:
  name: nginx-lb
spec:
  type: LoadBalancer
  selector:
    project: roboshop
    enviroment: dev
    component: frontend
    purpose: loadbalance
  ports:
    - protocol: TCP
      port: 80  #serviceport
      targetport: 80 #containerport
      nodeport: 30007