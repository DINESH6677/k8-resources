apiversion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    project: roboshop
    enviroment: dev
    component: frontend
  ports:
    - protocol: TCP
      port: 80  #serviceport
      targetport: 80 #containerport