apiversion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx
  # these are replica set labels20-Deployment.yaml
  
  labels:
    purpose: rs-demo
    project: roboshop
  spec:
    replicas: 3
    selector:
    # these labels are used to select pods
      matchlabels:
        purpose: rs-demo
        project: roboshop
        # pod definitn
    template:
      metadata:
        labels:
          purpose: rs-demo
          project: roboshop
      spec:
        containers:
        - name: nginx
          image: nginx