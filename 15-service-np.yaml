apiversion: v1
kind: Service
metadata:
  name: nginx-np
spec:
  type: NodePort
  selector:
    project: roboshop
    enviroment: dev
    component: frontend
    purpose: node-port
  ports:
    - protocol: TCP
      port: 80  #serviceport
      targetport: 80 #containerport