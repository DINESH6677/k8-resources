apiversion: v1
kind: pod
metadata:
  name: service-np-demo
  # these labels aare used to register this pod with service
  labels:
    project: roboshop
    enviroment: dev
    component: frontend
    purpose: node-port
spec:
  containers:
    - name: nginx
      image: nginx
